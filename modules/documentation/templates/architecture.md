# Architecture: {project_name}

{one_paragraph_summary — what the system does and why it is structured this way}

## System Context

```mermaid
C4Context
    title System Context — {project_name}
    Person(user, "{actor_name}", "{actor_description}")
    System(system, "{project_name}", "{system_description}")
    System_Ext(ext1, "{external_system_1}", "{ext_description_1}")

    Rel(user, system, "{interaction}")
    Rel(system, ext1, "{interaction}")
```

## Component Overview

```mermaid
C4Component
    title Components — {project_name}
    Container_Boundary(app, "{app_name}") {
        Component(comp1, "{component_1}", "{tech_1}", "{responsibility_1}")
        Component(comp2, "{component_2}", "{tech_2}", "{responsibility_2}")
    }
    Rel(comp1, comp2, "{interaction}")
```

## Layers

| Layer | Responsibility | Key types |
|-------|---------------|-----------|
| {layer_1} | {responsibility_1} | {types_1} |
| {layer_2} | {responsibility_2} | {types_2} |

## Data Flow

{description of primary data flow through the system}

```mermaid
sequenceDiagram
    participant {actor}
    participant {service_1}
    participant {service_2}

    {actor}->>+{service_1}: {request}
    {service_1}->>+{service_2}: {call}
    {service_2}-->>-{service_1}: {response}
    {service_1}-->>-{actor}: {result}
```

## Key Design Decisions

See [docs/adr/](docs/adr/) for individual ADRs.

| Decision | ADR | Summary |
|----------|-----|---------|
| {decision_1} | [ADR-{nnn}](docs/adr/adr-{nnn}-{slug}.md) | {one_line_summary} |

## Dependencies

{description of external dependencies and why each is included}

<!-- user-maintained -->

<!-- /user-maintained -->

<!-- generated by dev-pipeline docs-generator -->
<!-- Last updated: {date} -->
