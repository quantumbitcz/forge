# v1.17 Cost & Efficiency — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reduce forge pipeline token costs by 60-80% through model routing, caching, and context isolation while providing measurement infrastructure.

**Architecture:** Five independent features sharing the orchestrator as integration point. Token budget reporting (Feature 5) provides measurement infrastructure, model routing (Feature 1) uses it, context isolation (Feature 4) optimizes the review stage, and two caching features (Features 2 & 3) reduce EXPLORE and PLAN costs. All features are additive — disabled by default in config, no breaking changes.

**Tech Stack:** Markdown (agent contracts), Bash (forge-token-tracker.sh), JSON (state schema, category registry), BATS (structural tests)

**Implementation Order:** Feature 5 → 1 → 4 → 2 → 3 → Cross-cutting (config templates, CLAUDE.md, tests)

---

### Task 1: Create shared/model-routing.md contract

This is the foundational contract that defines how model routing works across the pipeline.

**Files:**
- Create: `shared/model-routing.md`

- [ ] **Step 1: Write structural test for the new contract**

Add a test case to verify `shared/model-routing.md` exists and contains required sections.

In `tests/structural/shared-contracts.bats`, add:

```bash
@test "model-routing.md exists and has required sections" {
  [[ -f "$PLUGIN_ROOT/shared/model-routing.md" ]]
  grep -q "## Tier Definitions" "$PLUGIN_ROOT/shared/model-routing.md"
  grep -q "## Resolution Order" "$PLUGIN_ROOT/shared/model-routing.md"
  grep -q "## PREFLIGHT Constraints" "$PLUGIN_ROOT/shared/model-routing.md"
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./tests/lib/bats-core/bin/bats tests/structural/shared-contracts.bats --filter "model-routing"`
Expected: FAIL — file does not exist

- [ ] **Step 3: Create shared/model-routing.md**

```markdown
# Model Routing

Defines how the orchestrator selects model tiers for agent dispatch. Read at PREFLIGHT from `forge-config.md`. Applied on every `Agent(...)` call.

## Tier Definitions

| Tier | Model Parameter | Use Case | Cost Ratio |
|------|----------------|----------|-----------|
| `fast` | `haiku` | Pattern matching, scaffolding, docs, deprecation refresh | ~0.05x |
| `standard` | `sonnet` | Default — most agents, reviewers, test gate | 1.0x (baseline) |
| `premium` | `opus` | Planning, validation, security review, architecture review, implementation, bug investigation | ~5x |

The `model` parameter on the Agent tool accepts exactly: `haiku`, `sonnet`, `opus`.

## Configuration

In `forge-config.md`:

```yaml
model_routing:
  enabled: true
  default_tier: standard
  overrides:
    tier_1_fast:
      - fg-310-scaffolder
      - fg-350-docs-generator
      - fg-130-docs-discoverer
      - fg-140-deprecation-refresh
    tier_3_premium:
      - fg-200-planner
      - fg-210-validator
      - fg-411-security-reviewer
      - fg-412-architecture-reviewer
      - fg-020-bug-investigator
      - fg-300-implementer
```

Agents not listed in any override use `default_tier`.

## Resolution Order

1. `forge-config.md` `model_routing.overrides.tier_*` — per-agent explicit tier
2. `forge-config.md` `model_routing.default_tier` — project default
3. Plugin default: `standard` (sonnet)

## Dispatch Integration

The orchestrator resolves the model tier for each agent **before** calling `Agent(...)`. The resolved model is passed as the `model` parameter:

```
Agent(
  subagent_type: "forge:fg-200-planner",
  model: "opus",
  prompt: "..."
)
```

When `model_routing.enabled` is `false`, the `model` parameter is omitted (Claude Code uses its default).

## Fallback Behavior

If the resolved model is unavailable at runtime (Claude Code returns an error mentioning model availability), the orchestrator:
1. Logs WARNING: "Model {tier} unavailable for {agent}, falling back to {default_tier}"
2. Retries with `default_tier`
3. If `default_tier` also fails, retries without `model` parameter (platform default)
4. Records the fallback in `state.json.tokens.model_fallbacks[]`

## PREFLIGHT Constraints

Validated at PREFLIGHT. If violated, log WARNING and use plugin defaults:

| Parameter | Valid Values | Default |
|-----------|-------------|---------|
| `model_routing.enabled` | `true`, `false` | `false` |
| `model_routing.default_tier` | `fast`, `standard`, `premium` | `standard` |
| `model_routing.overrides.tier_1_fast[]` | valid agent IDs from agent-registry.md | `[]` |
| `model_routing.overrides.tier_3_premium[]` | valid agent IDs from agent-registry.md | `[]` |

Agent IDs in overrides are validated against `shared/agent-registry.md`. Unknown agent IDs produce WARNING (typo protection) but do not fail PREFLIGHT.

## Retrospective Auto-Tuning

`fg-700-retrospective` may suggest tier changes based on:
- Finding quality per agent: if a `fast`-tier agent produces findings that are frequently overridden or have low confidence, suggest upgrading to `standard`
- Cost efficiency: if a `premium`-tier agent's findings are consistently identical to what a `standard`-tier run produces, suggest downgrading
- At most one model routing adjustment per run
- `default_tier` and `enabled` are never auto-tuned (intentional project decisions)

Auto-tuning respects `<!-- locked -->` fences in `forge-config.md`.

## Stage Notes

The orchestrator records model assignments in `stage_0_notes`:

```
## Model Routing
- Mode: explicit
- Default tier: standard (sonnet)
- Overrides: 4 fast, 6 premium, 28 standard
- Total agents: 38
```
```

- [ ] **Step 4: Run test to verify it passes**

Run: `./tests/lib/bats-core/bin/bats tests/structural/shared-contracts.bats --filter "model-routing"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shared/model-routing.md tests/structural/shared-contracts.bats
git commit -m "feat(v1.17): add model routing contract"
```

---

### Task 2: Extend state-schema.md with token and model tracking fields

Add the extended `tokens.by_stage`, `tokens.by_agent`, `tokens.model_distribution` field definitions and `cost.estimated_cost_usd` to the existing state schema.

**Files:**
- Modify: `shared/state-schema.md`

- [ ] **Step 1: Read current tokens section in state-schema.md**

Read `shared/state-schema.md` and locate the `"tokens": {` section (~line 139-149) and the `"cost": {` section (~line 139-142).

- [ ] **Step 2: Extend tokens object with by_stage structure, by_agent structure, model_distribution**

In the `tokens` section of the state.json schema, after `"by_agent": {},` add the following field documentation, and replace the empty objects with the populated example:

```json
"tokens": {
  "estimated_total": 0,
  "budget_ceiling": 2000000,
  "by_stage": {},
  "by_agent": {},
  "model_distribution": {},
  "model_fallbacks": [],
  "budget_warning_issued": false
}
```

Add a **field documentation table** below the schema:

```markdown
### tokens.by_stage entry schema

When populated by `forge-token-tracker.sh`, each stage entry has:

| Field | Type | Description |
|-------|------|-------------|
| `input` | integer | Input tokens consumed in this stage |
| `output` | integer | Output tokens generated in this stage |
| `agents` | string[] | Agent IDs dispatched in this stage |

### tokens.by_agent entry schema

| Field | Type | Description |
|-------|------|-------------|
| `input` | integer | Input tokens consumed by this agent |
| `output` | integer | Output tokens generated by this agent |
| `model` | string | Model used for this agent dispatch (`haiku`, `sonnet`, `opus`, or empty) |

### tokens.model_distribution

Object mapping model names to usage fractions. Example: `{ "haiku": 0.35, "sonnet": 0.45, "opus": 0.20 }`. Computed by `forge-token-tracker.sh` after each stage.

### tokens.model_fallbacks

Array of fallback events. Each entry: `{ "agent": "fg-200-planner", "requested": "opus", "actual": "sonnet", "reason": "model unavailable" }`.
```

- [ ] **Step 3: Extend cost object with estimated_cost_usd**

In the `cost` section, add:

```json
"cost": {
  "wall_time_seconds": 0,
  "stages_completed": 0,
  "estimated_cost_usd": 0.0
}
```

Add field documentation:

```markdown
### cost.estimated_cost_usd

Estimated USD cost based on token consumption and model pricing. Computed by `forge-token-tracker.sh` using approximate pricing: haiku ~$0.25/MTok input, sonnet ~$3/MTok input, opus ~$15/MTok input. Updated after each stage.
```

- [ ] **Step 4: Add explore-cache.json and agent-card.json to directory structure**

In the directory structure section at the top of state-schema.md, add:

```
.forge/
+-- explore-cache.json                  # Codebase exploration cache (persisted across runs)
+-- plan-cache/                         # Cached PLAN outputs for similarity matching
|   +-- index.json                      # Plan cache index
|   +-- plan-{date}-{slug}.json         # Individual cached plans
```

Add to the file lifecycle table:

| File | Created At | Updated By | Survives Conversation? | Committed to Git? |
|------|-----------|-----------|----------------------|-------------------|
| `explore-cache.json` | Stage 1 (EXPLORE) | Orchestrator | Yes (survives /forge-reset) | No |
| `plan-cache/*.json` | Stage 2 (PLAN) | Orchestrator | Yes (survives /forge-reset) | No |

- [ ] **Step 5: Commit**

```bash
git add shared/state-schema.md
git commit -m "feat(v1.17): extend state schema with token tracking and cache structures"
```

---

### Task 3: Extend forge-token-tracker.sh with per-stage/agent/model tracking

**Files:**
- Modify: `shared/forge-token-tracker.sh`

- [ ] **Step 1: Read the current script**

Read `shared/forge-token-tracker.sh` fully to understand the `do_record()` function structure and the `jq` patterns used to update state.json.

- [ ] **Step 2: Extend do_record() argument parsing to accept model parameter**

The current signature is approximately: `forge-token-tracker.sh record <stage> <agent> <input> <output>`. Add an optional 5th positional parameter `<model>`:

```bash
do_record() {
  local stage="${1:?stage required}"
  local agent="${2:?agent required}"
  local input_tokens="${3:?input required}"
  local output_tokens="${4:?output required}"
  local model="${5:-}"  # optional, empty if not provided
  # ... existing logic
```

- [ ] **Step 3: Add model to by_agent tracking in the jq update expression**

After the existing `by_agent` update, add model tracking:

```bash
  # Existing: update by_agent
  # New: include model in by_agent entry
  local jq_expr
  jq_expr=$(cat <<'JQ'
    .tokens.by_stage[$stage] //= {"input": 0, "output": 0, "agents": []}
    | .tokens.by_stage[$stage].input += ($input | tonumber)
    | .tokens.by_stage[$stage].output += ($output | tonumber)
    | .tokens.by_stage[$stage].agents += [$agent] | .tokens.by_stage[$stage].agents |= unique
    | .tokens.by_agent[$agent] //= {"input": 0, "output": 0, "model": ""}
    | .tokens.by_agent[$agent].input += ($input | tonumber)
    | .tokens.by_agent[$agent].output += ($output | tonumber)
    | .tokens.by_agent[$agent].model = (if $model != "" then $model else .tokens.by_agent[$agent].model end)
    | .tokens.estimated_total += (($input | tonumber) + ($output | tonumber))
JQ
  )
```

- [ ] **Step 4: Add model_distribution computation**

After updating by_agent, recompute model_distribution:

```bash
  # Recompute model_distribution from by_agent data
  local dist_expr
  dist_expr=$(cat <<'JQ'
    .tokens.model_distribution = (
      [.tokens.by_agent | to_entries[] | select(.value.model != "") | .value]
      | group_by(.model)
      | map({key: .[0].model, value: ([.[].input, .[].output] | add)})
      | (map(.value) | add) as $total
      | map({(.key): (if $total > 0 then (.value / $total) else 0 end)})
      | add // {}
    )
JQ
  )
```

- [ ] **Step 5: Add estimated_cost_usd computation**

After model_distribution, compute estimated cost:

```bash
  # Compute estimated USD cost from token usage and model pricing
  local cost_expr
  cost_expr=$(cat <<'JQ'
    .cost.estimated_cost_usd = (
      [.tokens.by_agent | to_entries[] | .value |
        (if .model == "haiku" then (.input * 0.00000025 + .output * 0.00000125)
         elif .model == "opus" then (.input * 0.000015 + .output * 0.000075)
         else (.input * 0.000003 + .output * 0.000015) end)
      ] | add // 0 | . * 100 | round / 100
    )
JQ
  )
```

- [ ] **Step 6: Run existing tests to verify no regression**

Run: `./tests/run-all.sh unit`
Expected: All existing token tracker tests pass

- [ ] **Step 7: Commit**

```bash
git add shared/forge-token-tracker.sh
git commit -m "feat(v1.17): extend token tracker with model and cost tracking"
```

---

### Task 4: Add model routing to orchestrator PREFLIGHT and dispatch

**Files:**
- Modify: `agents/fg-100-orchestrator.md`

- [ ] **Step 1: Read orchestrator PREFLIGHT section**

Read `agents/fg-100-orchestrator.md` lines around §0.3 (Read Mutable Runtime Params) and §0.4 (Config Validation) to find exact insertion points.

- [ ] **Step 2: Add §0.3a Model Route Resolution after config read**

After the orchestrator reads `forge-config.md` (§0.3), insert a new subsection:

```markdown
### §0.3a Model Route Resolution

Read `model_routing` from `forge-config.md`. If `model_routing.enabled` is `false` or absent, skip — all agents use platform default.

If enabled:
1. Build a model map: `agent_id → model_parameter`
   - For each agent in `tier_1_fast`: map to `haiku`
   - For each agent in `tier_3_premium`: map to `opus`
   - All other agents: map to the tier name from `default_tier` (`fast` → `haiku`, `standard` → `sonnet`, `premium` → `opus`)
2. Validate agent IDs against `shared/agent-registry.md`. Unknown IDs: log WARNING, skip.
3. Store the model map in orchestrator context (not in state.json — ephemeral per-run).
4. Record summary in `stage_0_notes`:
   ```
   ## Model Routing
   - Enabled: true
   - Default tier: standard (sonnet)
   - Fast tier (haiku): fg-310-scaffolder, fg-350-docs-generator, fg-130-docs-discoverer, fg-140-deprecation-refresh
   - Premium tier (opus): fg-200-planner, fg-210-validator, fg-411-security-reviewer, fg-412-architecture-reviewer, fg-020-bug-investigator, fg-300-implementer
   - Standard tier (sonnet): all remaining agents
   ```
```

- [ ] **Step 3: Add model parameter to all Agent dispatch calls**

In each stage's dispatch section, update the Agent call pattern. Before (current):

```
Agent(
  subagent_type: "forge:fg-200-planner",
  prompt: "..."
)
```

After:

```
Agent(
  subagent_type: "forge:fg-200-planner",
  model: <resolved from model map, omit if model_routing.enabled is false>,
  prompt: "..."
)
```

Add this note to the dispatch pattern section:

```markdown
**Model parameter:** When `model_routing.enabled`, include `model: <tier>` in every Agent dispatch. The model map is built at §0.3a. When disabled, omit the `model` parameter entirely (do not pass `model: "sonnet"` — let the platform decide).
```

- [ ] **Step 4: Add model fallback handling**

In the orchestrator's error handling section (recovery from agent dispatch failures), add:

```markdown
**Model fallback:** If an agent dispatch fails with a model-related error (message contains "model" and "unavailable" or "not supported"):
1. Log WARNING: "Model {model} unavailable for {agent}, retrying with default tier"
2. Record in `state.json.tokens.model_fallbacks[]`: `{ "agent": "{agent}", "requested": "{model}", "actual": "{default_tier}", "reason": "unavailable" }`
3. Retry dispatch without `model` parameter
4. This does NOT consume a recovery budget point — model fallback is expected operational behavior
```

- [ ] **Step 5: Add token tracking call after each agent dispatch**

After each Agent dispatch returns, the orchestrator calls the token tracker:

```markdown
**Post-dispatch token tracking:** After every Agent dispatch returns, call:
```bash
shared/forge-token-tracker.sh record <stage_name> <agent_id> <input_tokens> <output_tokens> <model>
```
Where `<input_tokens>` and `<output_tokens>` are from the Agent tool's usage metadata, and `<model>` is the resolved model tier (or empty if model routing disabled).
```

- [ ] **Step 6: Commit**

```bash
git add agents/fg-100-orchestrator.md
git commit -m "feat(v1.17): add model routing to orchestrator PREFLIGHT and dispatch"
```

---

### Task 5: Add category affinity to category-registry.json

**Files:**
- Modify: `shared/checks/category-registry.json`

- [ ] **Step 1: Read current registry**

Read `shared/checks/category-registry.json` (25 lines).

- [ ] **Step 2: Add affinity field to every category**

Add an `"affinity"` array to each category entry. Categories without explicit affinity get `[]` (meaning: sent to all reviewers — backward compatible).

```json
{
  "_note": "Canonical registry of finding category codes. Source of truth for scoring.md, output-format.md, and all review agents. When adding a new category, update this registry first.",
  "categories": {
    "ARCH": { "description": "Architectural violation (SRP, DIP, layer boundary)", "agents": ["fg-412-architecture-reviewer"], "wildcard": true, "priority": 2, "affinity": ["fg-412-architecture-reviewer"] },
    "SEC": { "description": "Security concern (auth, injection, exposure)", "agents": ["fg-411-security-reviewer"], "wildcard": true, "priority": 1, "affinity": ["fg-411-security-reviewer"] },
    "PERF": { "description": "Backend performance issue (N+1, O(n^2), blocking I/O)", "agents": ["fg-416-backend-performance-reviewer"], "wildcard": true, "priority": 4, "affinity": ["fg-416-backend-performance-reviewer"] },
    "FE-PERF": { "description": "Frontend performance issue (re-renders, bundle size, DOM)", "agents": ["fg-413-frontend-reviewer"], "wildcard": true, "priority": 4, "affinity": ["fg-413-frontend-reviewer"] },
    "TEST": { "description": "Test quality issue (coverage, mocks, assertions, isolation)", "agents": ["fg-410-code-reviewer"], "wildcard": true, "priority": 3, "affinity": ["fg-410-code-reviewer"] },
    "CONV": { "description": "Convention violation (naming, style, patterns)", "agents": ["fg-410-code-reviewer"], "wildcard": true, "priority": 5, "affinity": ["fg-410-code-reviewer", "fg-413-frontend-reviewer"] },
    "DOC": { "description": "Documentation gap (missing docs, stale sections, cross-ref conflicts)", "agents": ["fg-418-docs-consistency-reviewer"], "wildcard": true, "priority": 5, "affinity": ["fg-418-docs-consistency-reviewer"] },
    "QUAL": { "description": "Code quality (complexity, duplication, dead code, error handling)", "agents": ["fg-410-code-reviewer"], "wildcard": true, "priority": 3, "affinity": ["fg-410-code-reviewer"] },
    "APPROACH": { "description": "Solution quality (suboptimal pattern, unnecessary complexity)", "agents": ["fg-410-code-reviewer"], "wildcard": true, "score_impact": "INFO_ONLY", "accumulation_rule": "3+ recurrences escalate to convention", "priority": 6, "affinity": [] },
    "SCOUT": { "description": "Boy Scout improvement (tracked, no point deduction)", "agents": ["fg-410-code-reviewer"], "wildcard": true, "score_impact": "NONE", "priority": 6, "affinity": [] },
    "A11Y": { "description": "Accessibility violation (WCAG, keyboard nav, screen reader, ARIA)", "agents": ["fg-413-frontend-reviewer"], "wildcard": true, "priority": 5, "affinity": ["fg-413-frontend-reviewer"] },
    "DEP": { "description": "Dependency health (vulnerable, unmaintained, outdated, conflicts, license)", "agents": ["fg-420-dependency-reviewer"], "wildcard": true, "priority": 5, "affinity": ["fg-420-dependency-reviewer"] },
    "COMPAT": { "description": "Compatibility issue (browser, platform, API version)", "agents": [], "wildcard": true, "status": "reserved", "priority": 5, "affinity": [] },
    "CONTRACT": { "description": "Contract validation (breaking changes, schema violations)", "agents": ["fg-250-contract-validator"], "wildcard": true, "priority": 3, "affinity": [] },
    "STRUCT": { "description": "Project structure violation (file placement, naming, boundaries)", "agents": ["fg-412-architecture-reviewer"], "wildcard": true, "priority": 2, "affinity": ["fg-412-architecture-reviewer"] },
    "INFRA": { "description": "Infrastructure/deployment issue (health, smoke, contract, image)", "agents": ["fg-419-infra-deploy-reviewer"], "wildcard": true, "priority": 4, "affinity": ["fg-419-infra-deploy-reviewer"] },
    "REVIEW-GAP": { "description": "Coverage gap from timed-out or failed review agent", "agents": ["fg-400-quality-gate"], "wildcard": false, "priority": 5, "affinity": [] },
    "DESIGN-TOKEN": { "description": "Frontend design token violation (hardcoded hex/rgb instead of theme tokens)", "agents": ["fg-413-frontend-reviewer"], "wildcard": false, "priority": 5, "affinity": ["fg-413-frontend-reviewer"] },
    "DESIGN-MOTION": { "description": "Frontend animation performance issue (non-GPU properties, missing will-change)", "agents": ["fg-413-frontend-reviewer"], "wildcard": false, "priority": 5, "affinity": ["fg-413-frontend-reviewer"] }
  }
}
```

**Key decisions:**
- `APPROACH` and `SCOUT`: `affinity: []` — these are cross-cutting and go to all reviewers
- `CONTRACT` and `COMPAT`: `affinity: []` — reserved/cross-cutting
- `REVIEW-GAP`: `affinity: []` — meta-finding, relevant to all
- `QUAL`: `affinity: ["fg-410-code-reviewer"]` — parent category. Subcategories (`QUAL-ERR-*`, `QUAL-COMPLEX`) have additional affinities documented in `agent-communication.md`

- [ ] **Step 3: Run structural tests**

Run: `./tests/run-all.sh structural`
Expected: All 51+ structural checks pass (JSON is valid)

- [ ] **Step 4: Commit**

```bash
git add shared/checks/category-registry.json
git commit -m "feat(v1.17): add category affinity field for context isolation"
```

---

### Task 6: Update agent-communication.md section 3 with domain-scoped dedup

**Files:**
- Modify: `shared/agent-communication.md`

- [ ] **Step 1: Read current section 3**

Read `shared/agent-communication.md` lines 47-98 (section 3: Shared Findings Context).

- [ ] **Step 2: Add domain-scoped dedup subsection after the existing dedup hints**

After the existing "Deduplication Hint Size Cap" paragraph (around line 66), add:

```markdown
### Domain-Scoped Deduplication Hints

When dispatching batch 2+ agents, the quality gate filters dedup hints by **category affinity** (defined in `shared/checks/category-registry.json`, `affinity` field). Each reviewer only sees findings from categories relevant to its domain:

- `fg-411-security-reviewer` receives: `SEC-*`, `QUAL-ERR-*` (error handling has security implications)
- `fg-412-architecture-reviewer` receives: `ARCH-*`, `STRUCT-*`, `QUAL-COMPLEX`
- `fg-413-frontend-reviewer` receives: `FE-PERF-*`, `A11Y-*`, `CONV-*` (frontend conventions), `DESIGN-*`
- `fg-416-backend-performance-reviewer` receives: `PERF-*`
- `fg-418-docs-consistency-reviewer` receives: `DOC-*`
- `fg-419-infra-deploy-reviewer` receives: `INFRA-*`
- `fg-420-dependency-reviewer` receives: `DEP-*`
- `fg-410-code-reviewer` receives: `TEST-*`, `CONV-*`, `QUAL-*`

**Affinity resolution:** For each finding in the dedup hints:
1. Look up the finding's category prefix in `category-registry.json`
2. Check the `affinity` array for the target reviewer's agent ID
3. If the reviewer is in the affinity list, OR if affinity is `[]` (empty — send to all): include the finding
4. Otherwise: exclude the finding from this reviewer's dedup hints

**Subcategory affinity:** Subcategories (e.g., `QUAL-ERR-*`) may have different affinity than their parent (`QUAL-*`). When a subcategory has explicit affinity documented here, use the subcategory's affinity. Otherwise, fall through to the parent category's affinity in `category-registry.json`.

Subcategory affinity overrides:
- `QUAL-ERR-*`: `["fg-410-code-reviewer", "fg-411-security-reviewer"]` (error handling is a security concern)
- `QUAL-COMPLEX`: `["fg-410-code-reviewer", "fg-412-architecture-reviewer"]` (complexity is an architecture concern)

**Backward compatibility:** If `affinity` is missing for a category in the registry, the finding is sent to ALL reviewers (pre-v1.17 behavior).
```

- [ ] **Step 3: Commit**

```bash
git add shared/agent-communication.md
git commit -m "feat(v1.17): add domain-scoped dedup hints for context isolation"
```

---

### Task 7: Update quality gate with domain-scoped dispatch logic

**Files:**
- Modify: `agents/fg-400-quality-gate.md`

- [ ] **Step 1: Read current batch dispatch section**

Read `agents/fg-400-quality-gate.md` around §5.2 (Inter-Batch Finding Deduplication) and §5.3 (Agent Dispatch Prompt).

- [ ] **Step 2: Add affinity filtering to §5.2**

In §5.2, after the existing dedup hint mechanism, add:

```markdown
**Domain-scoped filtering (v1.17+):** Before including dedup hints in a batch 2+ dispatch, filter by category affinity:

1. Read `shared/checks/category-registry.json` `affinity` field for each finding's category
2. For the target reviewer agent, include only findings where:
   - The reviewer's agent ID is in the category's `affinity` array, OR
   - The category's `affinity` is empty `[]` (cross-cutting finding)
3. Check subcategory overrides in `shared/agent-communication.md` §3 (Domain-Scoped Deduplication Hints)
4. Apply the existing top-20-by-severity cap AFTER filtering (not before — the cap applies to the filtered set)

This ensures each reviewer receives only domain-relevant dedup context, reducing per-reviewer token consumption by ~60-80%.
```

- [ ] **Step 3: Add model parameter to §5.3 dispatch prompt**

In §5.3 (Agent Dispatch Prompt), add the model parameter:

```markdown
**Model selection:** If `model_routing.enabled` in `forge-config.md`, include the `model` parameter in every reviewer dispatch:

```
Agent(
  subagent_type: "forge:fg-411-security-reviewer",
  model: <resolved from orchestrator's model map>,
  prompt: "..."
)
```

The model map is passed to the quality gate via the orchestrator's dispatch prompt (not read from config directly — the quality gate is a sub-agent of the orchestrator).
```

- [ ] **Step 4: Commit**

```bash
git add agents/fg-400-quality-gate.md
git commit -m "feat(v1.17): add domain-scoped dedup filtering and model routing to quality gate"
```

---

### Task 8: Create shared/explore-cache.md contract

**Files:**
- Create: `shared/explore-cache.md`

- [ ] **Step 1: Write structural test**

In `tests/structural/shared-contracts.bats`, add:

```bash
@test "explore-cache.md exists and has required sections" {
  [[ -f "$PLUGIN_ROOT/shared/explore-cache.md" ]]
  grep -q "## Cache Schema" "$PLUGIN_ROOT/shared/explore-cache.md"
  grep -q "## Invalidation Rules" "$PLUGIN_ROOT/shared/explore-cache.md"
  grep -q "## PREFLIGHT Integration" "$PLUGIN_ROOT/shared/explore-cache.md"
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./tests/lib/bats-core/bin/bats tests/structural/shared-contracts.bats --filter "explore-cache"`
Expected: FAIL

- [ ] **Step 3: Create shared/explore-cache.md**

```markdown
# Explore Cache

Persists codebase exploration data across pipeline runs in `.forge/explore-cache.json`. On subsequent runs, only files changed since the last explored commit SHA are re-analyzed.

## Cache Schema

`.forge/explore-cache.json`:

```json
{
  "schema_version": "1.0.0",
  "last_explored_sha": "abc123def456",
  "conventions_hash": "ab12cd34",
  "file_index": {
    "src/domain/Plan.kt": {
      "hash": "sha256:...",
      "patterns": ["repository", "entity", "domain-object"],
      "dependencies": ["PlanRepository", "PlanService"],
      "module": "domain"
    }
  },
  "cache_age_runs": 3,
  "created_at": "2026-04-10T10:00:00Z",
  "last_updated_at": "2026-04-12T10:00:00Z"
}
```

| Field | Type | Description |
|-------|------|-------------|
| `schema_version` | string | Cache format version for migration |
| `last_explored_sha` | string | Git commit SHA of last full/partial explore |
| `conventions_hash` | string | SHA256 of `forge-config.md` conventions section at time of explore |
| `file_index` | object | Per-file exploration data keyed by project-relative path |
| `file_index.*.hash` | string | SHA256 of file content at time of indexing |
| `file_index.*.patterns` | string[] | Detected code patterns (entity, repository, controller, service, etc.) |
| `file_index.*.dependencies` | string[] | Key symbols this file depends on or exports |
| `file_index.*.module` | string | Detected module/layer (domain, infrastructure, api, etc.) |
| `cache_age_runs` | integer | Number of runs since last full re-explore |
| `created_at` | string | ISO 8601 timestamp of initial creation |
| `last_updated_at` | string | ISO 8601 timestamp of last update |

## Invalidation Rules

| Condition | Action |
|-----------|--------|
| `cache_age_runs > max_cache_age_runs` (config, default 10) | Full re-explore |
| `conventions_hash` differs from current `forge-config.md` hash | Full re-explore |
| Cache file missing or corrupt (invalid JSON, missing `schema_version`) | Full re-explore |
| `schema_version` mismatch with current plugin version | Full re-explore |
| `--full-explore` flag passed to `/forge-run` | Full re-explore |
| None of the above | Partial re-explore (changed files only) |

## Partial Re-Explore

When cache is valid:
1. Compute changed files: `git diff --name-only {last_explored_sha}..HEAD`
2. For each changed file: re-analyze and update `file_index` entry
3. For deleted files: remove from `file_index`
4. For new files: add to `file_index`
5. Update `last_explored_sha` to current HEAD
6. Increment `cache_age_runs`
7. Update `last_updated_at`

The EXPLORE stage agent receives the cached `file_index` as context, plus the list of changed files to focus on.

## PREFLIGHT Integration

At PREFLIGHT, the orchestrator:
1. Checks if `.forge/explore-cache.json` exists
2. If exists: validates schema, checks invalidation rules
3. If valid: passes cache data to EXPLORE dispatch prompt with instruction to do partial analysis
4. If invalid or missing: normal full EXPLORE (agent creates cache at completion)
5. Records cache status in `stage_0_notes`: "Explore cache: hit (12 changed files) / miss (full explore) / stale (conventions changed)"

## Configuration

In `forge-config.md`:

```yaml
explore:
  cache_enabled: true
  max_cache_age_runs: 10
```

| Parameter | Range | Default | Description |
|-----------|-------|---------|-------------|
| `cache_enabled` | boolean | `true` | Enable/disable explore caching |
| `max_cache_age_runs` | 1-50 | 10 | Runs before forced full re-explore |

## /forge-reset Behavior

`/forge-reset` preserves `explore-cache.json` (same as `docs-index.json` and `feedback/`). Only `/forge-reset --hard` or manual deletion removes it.
```

- [ ] **Step 4: Run test to verify it passes**

Run: `./tests/lib/bats-core/bin/bats tests/structural/shared-contracts.bats --filter "explore-cache"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shared/explore-cache.md tests/structural/shared-contracts.bats
git commit -m "feat(v1.17): add explore cache contract"
```

---

### Task 9: Create shared/plan-cache.md contract

**Files:**
- Create: `shared/plan-cache.md`

- [ ] **Step 1: Write structural test**

In `tests/structural/shared-contracts.bats`, add:

```bash
@test "plan-cache.md exists and has required sections" {
  [[ -f "$PLUGIN_ROOT/shared/plan-cache.md" ]]
  grep -q "## Cache Entry Schema" "$PLUGIN_ROOT/shared/plan-cache.md"
  grep -q "## Similarity Algorithm" "$PLUGIN_ROOT/shared/plan-cache.md"
  grep -q "## Eviction Rules" "$PLUGIN_ROOT/shared/plan-cache.md"
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./tests/lib/bats-core/bin/bats tests/structural/shared-contracts.bats --filter "plan-cache"`
Expected: FAIL

- [ ] **Step 3: Create shared/plan-cache.md**

```markdown
# Plan Cache

Caches PLAN stage outputs in `.forge/plan-cache/` for reuse when similar requirements arise. Before dispatching the planner, the orchestrator checks if a cached plan can serve as a starting point.

## Directory Structure

```
.forge/plan-cache/
+-- index.json
+-- plan-2026-04-10-add-comments.json
+-- plan-2026-04-08-auth-middleware.json
```

## Cache Entry Schema

Each `plan-{date}-{slug}.json`:

```json
{
  "schema_version": "1.0.0",
  "requirement": "Add plan comment feature with threading",
  "requirement_keywords": ["plan", "comment", "threading", "feature"],
  "domain_area": "plan",
  "plan_hash": "sha256:abc123",
  "stories_count": 4,
  "final_score": 94,
  "created_at": "2026-04-10T10:00:00Z",
  "source_sha": "def456",
  "plan_content": "... full plan markdown ..."
}
```

| Field | Type | Description |
|-------|------|-------------|
| `requirement` | string | Original requirement text |
| `requirement_keywords` | string[] | Extracted keywords (nouns, verbs, domain terms — lowercase, deduplicated) |
| `domain_area` | string | Detected domain from `shared/domain-detection.md` |
| `plan_hash` | string | SHA256 of `plan_content` |
| `stories_count` | integer | Number of stories in the plan |
| `final_score` | integer | Quality score the plan achieved (0 if run didn't complete) |
| `created_at` | string | ISO 8601 creation timestamp |
| `source_sha` | string | Git commit SHA when plan was created |
| `plan_content` | string | Full plan markdown content |

## Index Schema

`index.json`:

```json
{
  "schema_version": "1.0.0",
  "entries": [
    {
      "file": "plan-2026-04-10-add-comments.json",
      "requirement_keywords": ["plan", "comment", "threading"],
      "domain_area": "plan",
      "final_score": 94,
      "created_at": "2026-04-10T10:00:00Z"
    }
  ]
}
```

## Similarity Algorithm

1. Extract keywords from the new requirement: split on whitespace/punctuation, lowercase, remove stop words (the, a, an, is, are, to, for, in, on, of, with, and, or, by, from, this, that), deduplicate
2. For each entry in `index.json`:
   a. Compute Jaccard similarity: `|A ∩ B| / |A ∪ B|` where A = new keywords, B = cached keywords
   b. If `domain_area` matches: bonus +0.1 to similarity score
3. If highest similarity >= 0.6: offer cached plan as starting point
4. If multiple plans >= 0.6: use the one with highest `final_score`

**Known limitation:** Jaccard is brittle for short texts with varied wording. Acceptable for v1 — cache is an optimization, not a requirement. Future: embedding-based similarity.

## Orchestrator Integration

At PLAN stage, before dispatching `fg-200-planner`:
1. Check if `.forge/plan-cache/index.json` exists
2. If exists: run similarity algorithm against current requirement
3. If match found (>= 0.6): include cached plan in planner dispatch prompt:
   ```
   A similar requirement was previously planned. The cached plan is provided as a
   starting point — adapt it to the current requirement, do not copy it verbatim.
   
   Cached plan (from: {created_at}, score: {final_score}):
   {plan_content}
   ```
4. If no match: normal plan dispatch (no cached context)
5. Record cache status in `stage_2_notes`: "Plan cache: hit (similarity 0.73, domain: plan) / miss"

After PLAN completes successfully AND the run reaches SHIP:
1. Save current plan to `.forge/plan-cache/plan-{date}-{slug}.json`
2. Update `index.json`
3. Run eviction (see below)

Plans from runs that did not reach SHIP are NOT cached (the plan may be flawed).

## Eviction Rules

| Rule | Action |
|------|--------|
| Entries > 20 | Remove oldest by `created_at` (LRU) |
| Entry older than 30 days | Remove |
| `final_score` < `pass_threshold` | Remove (plan quality was insufficient) |

Eviction runs after each new plan is cached.

## Configuration

In `forge-config.md`:

```yaml
plan_cache:
  enabled: true
  similarity_threshold: 0.6
  max_entries: 20
  max_age_days: 30
```

## /forge-reset Behavior

`/forge-reset` preserves `.forge/plan-cache/` (same as explore cache). Only `/forge-reset --hard` or manual deletion removes it.
```

- [ ] **Step 4: Run test to verify it passes**

Run: `./tests/lib/bats-core/bin/bats tests/structural/shared-contracts.bats --filter "plan-cache"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shared/plan-cache.md tests/structural/shared-contracts.bats
git commit -m "feat(v1.17): add plan cache contract"
```

---

### Task 10: Update orchestrator with explore cache and plan cache integration

**Files:**
- Modify: `agents/fg-100-orchestrator.md`

- [ ] **Step 1: Read orchestrator PREFLIGHT and EXPLORE sections**

Read `agents/fg-100-orchestrator.md` — the PREFLIGHT section and the EXPLORE dispatch section.

- [ ] **Step 2: Add explore cache check to PREFLIGHT**

In the PREFLIGHT section, after workspace initialization (§0.12-§0.21), add:

```markdown
### §0.22a Explore Cache Check

1. Check if `.forge/explore-cache.json` exists
2. If exists:
   a. Parse JSON. If invalid: log WARNING "Corrupt explore cache, will do full explore", delete file
   b. Check `schema_version` matches expected (`1.0.0`)
   c. Check `conventions_hash` matches current `state.json.conventions_hash`
   d. Check `cache_age_runs < max_cache_age_runs` from config
   e. If all valid: set `explore_mode = "partial"`, compute changed files via `git diff --name-only {last_explored_sha}..HEAD`
   f. If any invalid: set `explore_mode = "full"`, log reason
3. If not exists: set `explore_mode = "full"`
4. Record in `stage_0_notes`: "Explore cache: {mode} ({reason}). Changed files: {count}"
```

- [ ] **Step 3: Modify EXPLORE dispatch to use cache**

In the EXPLORE stage dispatch section, add cache context:

```markdown
**EXPLORE dispatch with cache (§1.1a):**

If `explore_mode == "partial"`:
- Include in dispatch prompt: the cached `file_index` (as structured context) + list of changed files
- Instruct explorer: "Focus analysis on the {N} changed files listed below. For unchanged files, the cached patterns and dependencies are provided — verify they are still accurate by spot-checking 2-3 unchanged files."

If `explore_mode == "full"`:
- Normal EXPLORE dispatch (no cache context)
- After EXPLORE completes: write `.forge/explore-cache.json` with the exploration results
```

- [ ] **Step 4: Add plan cache check to PLAN stage**

In the PLAN stage dispatch section, add:

```markdown
**Plan cache check (§2.0a):**

Before dispatching `fg-200-planner`:
1. If `plan_cache.enabled` (default true) and `.forge/plan-cache/index.json` exists:
   a. Extract keywords from current requirement per `shared/plan-cache.md` algorithm
   b. Run similarity matching against index
   c. If match >= `similarity_threshold` (default 0.6): read the matched plan file
   d. Include cached plan in planner dispatch prompt (see `shared/plan-cache.md` §Orchestrator Integration)
2. If no match or cache disabled: normal dispatch

After SHIP stage completes successfully:
1. Save current plan to `.forge/plan-cache/plan-{date}-{slug}.json`
2. Update `index.json`
3. Run eviction per `shared/plan-cache.md` §Eviction Rules
```

- [ ] **Step 5: Commit**

```bash
git add agents/fg-100-orchestrator.md
git commit -m "feat(v1.17): add explore cache and plan cache integration to orchestrator"
```

---

### Task 11: Update retrospective and post-run agents

**Files:**
- Modify: `agents/fg-700-retrospective.md`
- Modify: `agents/fg-710-post-run.md`

- [ ] **Step 1: Read retrospective auto-tuning section**

Read `agents/fg-700-retrospective.md` around §2e (Auto-Tuning Rules).

- [ ] **Step 2: Add model routing auto-tuning rule**

After rule 9 in §2e, add:

```markdown
**Rule 10 — Model tier optimization:** If `model_routing.enabled` and `state.json.tokens.by_agent` data is available for 3+ runs:
- If a `fast`-tier (haiku) agent's findings have > 30% false positive rate (from `decision_quality`): suggest upgrading to `standard`. Log: "Agent {id} on haiku tier has high false positive rate ({rate}%). Consider upgrading to standard tier."
- If a `premium`-tier (opus) agent's finding quality is indistinguishable from `standard`-tier runs of the same agent type: suggest downgrading to `standard`. Log: "Agent {id} on opus tier shows no quality improvement over sonnet. Consider downgrading to save costs."
- At most one model routing adjustment per run
- `model_routing.enabled` and `model_routing.default_tier` are never auto-tuned
```

- [ ] **Step 3: Add model effectiveness analysis section**

After §2f, add:

```markdown
### §2g Model Effectiveness Analysis

When `model_routing.enabled`:
1. Read `state.json.tokens.by_agent` for model assignments
2. Read `state.json.tokens.model_distribution` for usage ratios
3. Compute cost-per-finding for each model tier:
   - `cost_per_finding[tier] = tokens_used[tier] / findings_produced[tier]`
4. Log in `forge-log.md` run entry:
   ```
   **Model routing:** haiku 35% / sonnet 45% / opus 20% | est. $2.40 (vs ~$6.80 all-opus baseline)
   ```
5. If `model_fallbacks` is non-empty: log each fallback event for visibility
```

- [ ] **Step 4: Read post-run recap template**

Read `agents/fg-710-post-run.md` around the recap template section (Part B).

- [ ] **Step 5: Add model and token summary to recap template**

In the recap template (Part B), after the "Metrics" section, add:

```markdown
## Token & Cost Summary

| Stage | Tokens (in/out) | Model | Duration |
|-------|----------------|-------|----------|
| PREFLIGHT | {in}K / {out}K | {model} | {dur} |
| EXPLORE | {in}K / {out}K | {model} | {dur} |
| ... | ... | ... | ... |

**Total:** {total_in}K in / {total_out}K out | Est. cost: ${cost}
**Model distribution:** haiku {pct}% / sonnet {pct}% / opus {pct}%

Read from `state.json.tokens` and `state.json.cost`. If `model_routing.enabled` is false, omit the model column and distribution line.
```

- [ ] **Step 6: Commit**

```bash
git add agents/fg-700-retrospective.md agents/fg-710-post-run.md
git commit -m "feat(v1.17): add model routing analysis to retrospective and token summary to recap"
```

---

### Task 12: Update agent-defaults.md with model routing guidelines

**Files:**
- Modify: `shared/agent-defaults.md`

- [ ] **Step 1: Read current agent-defaults.md**

Read `shared/agent-defaults.md` (75 lines).

- [ ] **Step 2: Add Model Routing section after Common Principles**

After the "Convention Stack Layers" section at the end, add:

```markdown
## Model Routing

When the orchestrator dispatches an agent with a `model` parameter (via `Agent(model: "haiku")`), the agent runs on the specified model tier. Agents do not control their own model selection.

### Agent Responsibilities

- **Do not** attempt to override or change the model assignment
- **Do not** adjust behavior based on perceived model capability (the orchestrator chose the tier based on the agent's task complexity)
- **Do** record the model assignment in stage notes if producing diagnostic output
- **Do** report if the task requires capabilities the assigned model may lack (e.g., a haiku-tier agent encountering a task that requires deep architectural reasoning should note this in stage notes, not silently produce lower-quality output)

### Orchestrator Responsibilities

- Resolve model tier from `forge-config.md` `model_routing` section per `shared/model-routing.md`
- Pass `model` parameter on every `Agent(...)` dispatch when `model_routing.enabled`
- Handle fallbacks when a model is unavailable
- Track token usage per agent per model via `shared/forge-token-tracker.sh`
```

- [ ] **Step 3: Commit**

```bash
git add shared/agent-defaults.md
git commit -m "feat(v1.17): add model routing guidelines to agent defaults"
```

---

### Task 13: Update forge-reset skill to preserve caches

The current `/forge-reset` does `rm -rf .forge/` which would delete the explore cache and plan cache. Per the spec, these should survive resets.

**Files:**
- Modify: `skills/forge-reset/SKILL.md`

- [ ] **Step 1: Read current forge-reset skill**

Read `skills/forge-reset/SKILL.md` — focus on the deletion logic.

- [ ] **Step 2: Change deletion from rm -rf to selective cleanup**

Replace the `rm -rf .forge/` approach with selective deletion that preserves caches. In the "What to do" section, replace step 2's cleanup:

Current (line 41):
```
- Remove `.forge/` directory: `rm -rf .forge/`
```

Replace with:
```markdown
   ## Cleanup (selective — preserves caches)

   Remove run state files but preserve cross-run caches:
   ```bash
   # Remove run state
   rm -f .forge/state.json
   rm -f .forge/checkpoint-*.json
   rm -f .forge/stage_*_notes_*.md
   rm -f .forge/stage_final_notes_*.md
   rm -f .forge/.lock
   rm -f .forge/.check-engine-skipped
   rm -rf .forge/reports/
   rm -rf .forge/feedback/
   rm -rf .forge/tracking/
   rm -rf .forge/progress/
   rm -f .forge/evidence.json
   rm -f .forge/decisions.jsonl
   rm -f .forge/automation-log.jsonl

   # Preserve (do NOT delete):
   # .forge/explore-cache.json — cross-run codebase index
   # .forge/plan-cache/ — cross-run plan cache
   # .forge/docs-index.json — documentation index
   # .forge/wiki/ — auto-generated wiki (v1.20)
   # .forge/agent-card.json — A2A agent card (v1.19)
   ```

   For `--hard` reset (full wipe including caches): `rm -rf .forge/`
```

- [ ] **Step 3: Update "What gets deleted" section**

Replace:
```
Deletes: state.json, checkpoint-*.json, stage_*_notes_*.md, stage_final_notes_*.md, .lock, .check-engine-skipped, reports/, feedback/ (but NOT forge-log.md, forge-config.md, or forge.local.md — these survive resets)
```

With:
```
Deletes: state.json, checkpoint-*.json, stage_*_notes_*.md, stage_final_notes_*.md, .lock, .check-engine-skipped, reports/, feedback/, tracking/, progress/, evidence.json, decisions.jsonl

Preserves (inside .forge/): explore-cache.json, plan-cache/, docs-index.json, wiki/, agent-card.json

Does NOT touch (outside .forge/): forge-log.md, forge.local.md, forge-config.md
```

- [ ] **Step 4: Commit**

```bash
git add skills/forge-reset/SKILL.md
git commit -m "feat(v1.17): update forge-reset to preserve cross-run caches"
```

---

### Task 14: Update forge-config-template.md files with new config sections

**Files:**
- Modify: All 21 `modules/frameworks/*/forge-config-template.md` files

- [ ] **Step 1: Identify all template files**

Run: `find modules/frameworks -name "forge-config-template.md" | sort`

- [ ] **Step 2: Add v1.17 configuration sections to each template**

Append the following to each `forge-config-template.md`, after the existing convergence section:

```markdown
## Model Routing

    model_routing:
      enabled: false
      default_tier: standard
      overrides:
        tier_1_fast: []
        tier_3_premium: []

## Explore Cache

    explore:
      cache_enabled: true
      max_cache_age_runs: 10

## Plan Cache

    plan_cache:
      enabled: true
      similarity_threshold: 0.6
      max_entries: 20
      max_age_days: 30
```

- [ ] **Step 3: Run structural tests to verify templates are valid**

Run: `./tests/run-all.sh structural`
Expected: PASS — templates have required sections

- [ ] **Step 4: Commit**

```bash
git add modules/frameworks/*/forge-config-template.md
git commit -m "feat(v1.17): add model routing, explore cache, plan cache config to all framework templates"
```

---

### Task 15: Update CLAUDE.md and plugin.json for v1.17

**Files:**
- Modify: `CLAUDE.md`
- Modify: `plugin.json`
- Modify: `marketplace.json`

- [ ] **Step 1: Update plugin.json version**

Change version from `1.16.0` to `1.17.0`.

- [ ] **Step 2: Update marketplace.json version**

Change version from `1.16.0` to `1.17.0`.

- [ ] **Step 3: Update CLAUDE.md**

In CLAUDE.md, update:
1. Version reference: `1.16.0` → `1.17.0`
2. In the "Key entry points" table, add:
   ```
   | Model routing | `shared/model-routing.md` |
   | Explore cache | `shared/explore-cache.md` |
   | Plan cache | `shared/plan-cache.md` |
   ```
3. In the "Supporting systems" section, add:
   ```
   - **Model routing** (`model-routing.md`): Tiered model selection per agent. Config: `model_routing.*` in `forge-config.md`.
   - **Explore cache** (`explore-cache.md`): Incremental codebase indexing across runs. `.forge/explore-cache.json`.
   - **Plan cache** (`plan-cache.md`): Keyword-based plan reuse. `.forge/plan-cache/`.
   ```
4. In the "Gotchas" section under "Structural", add:
   ```
   - `explore-cache.json` and `plan-cache/` survive `/forge-reset`. Only `--hard` removes them.
   ```
5. In the configuration section, add the new config parameters with their PREFLIGHT constraint ranges.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md plugin.json marketplace.json
git commit -m "feat(v1.17): update CLAUDE.md, plugin.json, marketplace.json for v1.17.0"
```

---

### Task 16: Run full test suite and fix any failures

**Files:**
- Possibly modify: any file with test failures

- [ ] **Step 1: Run full test suite**

Run: `./tests/run-all.sh`
Expected: All tests pass. If any fail, investigate and fix.

- [ ] **Step 2: Run structural validation**

Run: `./tests/validate-plugin.sh`
Expected: All 51+ structural checks pass

- [ ] **Step 3: Fix any failures**

If any tests fail:
1. Read the failure output
2. Identify the root cause (missing section, broken JSON, wrong format)
3. Fix the issue
4. Re-run the failing test
5. Re-run the full suite to confirm no regressions

- [ ] **Step 4: Commit fixes (if any)**

```bash
git add -A
git commit -m "fix(v1.17): address test failures from v1.17 changes"
```

---

### Task 17: Final release commit

**Files:**
- None new — this is a verification and tagging step

- [ ] **Step 1: Verify all changes are committed**

Run: `git status` — should be clean

- [ ] **Step 2: Review the full diff since v1.16.0**

Run: `git diff v1.16.0..HEAD --stat` to verify all expected files were modified

- [ ] **Step 3: Create release commit**

```bash
git commit --allow-empty -m "release: v1.17.0 — cost efficiency (model routing, explore cache, plan cache, context isolation, token reporting)"
```

- [ ] **Step 4: Tag the release**

```bash
git tag v1.17.0
```
