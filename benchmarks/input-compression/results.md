# Input Compression Benchmark Results

Run `./measure.sh` to populate this file with actual measurements.

```bash
cd benchmarks/input-compression
./measure.sh              # Default: aggressive (level 2)
./measure.sh --level 1    # Conservative
./measure.sh --level 3    # Ultra
```
