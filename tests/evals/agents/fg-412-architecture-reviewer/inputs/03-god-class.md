# Eval: God class with too many responsibilities

## Language: python

## Context
Single class handles user management, email sending, logging, and database operations.

## Code Under Review

```python
# file: src/services/user_manager.py
class UserManager:
    def create_user(self, data): ...
    def delete_user(self, id): ...
    def update_user(self, id, data): ...
    def send_welcome_email(self, user): ...
    def send_password_reset(self, user): ...
    def log_action(self, action, user): ...
    def export_to_csv(self, users): ...
    def generate_report(self, filters): ...
    def validate_password(self, password): ...
    def sync_to_crm(self, user): ...
    def calculate_subscription(self, user): ...
    def process_payment(self, user, amount): ...
```

## Expected Behavior
Reviewer should flag this as a god class with too many unrelated responsibilities (SRP violation).
