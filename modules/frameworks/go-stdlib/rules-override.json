{
  "_version": 1,
  "_updated": "2026-04-10T00:00:00Z",
  "extends": "go",
  "framework": null,
  "additional_rules": [
    {
      "id": "GS-CTX-001",
      "name": "missing-context-parameter",
      "pattern": "func\\s+\\w+\\([^)]*\\)\\s",
      "scope_pattern": "internal/",
      "severity": "INFO",
      "category": "CONV-CTX",
      "message": "Exported function without context.Context parameter \u2014 consider adding ctx for I/O operations",
      "fix_hint": "Add `ctx context.Context` as the first parameter if this function performs any I/O (DB, HTTP, file).",
      "example_ref": ""
    },
    {
      "id": "GS-ERR-001",
      "name": "capitalized-error-string",
      "pattern": "errors\\.New\\(\"[A-Z]|fmt\\.Errorf\\(\"[A-Z]",
      "scope_pattern": "",
      "severity": "INFO",
      "category": "CONV-ERR",
      "message": "Error string starts with capital letter \u2014 Go convention is lowercase, no trailing punctuation",
      "fix_hint": "Start error strings with a lowercase letter and omit trailing periods: `errors.New(\"not found\")` instead of `errors.New(\"Not found.\")`.",
      "example_ref": ""
    },
    {
      "id": "GS-CONV-001",
      "name": "panic-in-application",
      "pattern": "\\bpanic\\(",
      "scope_pattern": "internal/",
      "severity": "WARNING",
      "category": "CONV-SAFETY",
      "message": "panic() in application code \u2014 return errors instead of panicking",
      "fix_hint": "Replace panic() with returning an error. Only use panic in main() or test helpers.",
      "example_ref": ""
    },
    {
      "id": "GS-PLACE-001",
      "name": "dto-struct-in-handler",
      "pattern": "^type\\s+\\w+(Request|Response|DTO|Params|Input|Output)\\s+struct\\s*\\{",
      "scope_pattern": "internal/handler/",
      "severity": "WARNING",
      "category": "ARCH-PLACE",
      "message": "Request/Response struct defined in handler file \u2014 move to model/ or a dedicated dto/ package",
      "fix_hint": "Extract the struct to internal/model/ (domain types) or a handler-level dto package. Handler structs (e.g., UserHandler) are fine in handler files.",
      "example_ref": ""
    },
    {
      "id": "GS-PLACE-002",
      "name": "sql-query-in-handler",
      "pattern": "(SELECT|INSERT|UPDATE|DELETE|CREATE TABLE)\\s+",
      "scope_pattern": "internal/handler/",
      "severity": "CRITICAL",
      "category": "ARCH-PLACE",
      "message": "SQL query in handler file \u2014 database access belongs in repository layer",
      "fix_hint": "Move the SQL query to a repository method in internal/repository/.",
      "example_ref": ""
    }
  ],
  "additional_boundaries": [
    {
      "name": "Handlers must not import repositories",
      "scope_pattern": "internal/handler/",
      "forbidden_imports": [
        "internal/repository"
      ],
      "severity": "CRITICAL",
      "category": "ARCH-BOUNDARY"
    }
  ],
  "disabled_rules": [],
  "severity_overrides": {},
  "threshold_overrides": {
    "file_size": {
      "model/": 150,
      "handler/": 300
    }
  }
}
