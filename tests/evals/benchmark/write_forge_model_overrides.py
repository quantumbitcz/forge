"""Phase 8 benchmark forge-model override writer (stub — filled by later tasks)."""
from __future__ import annotations

from pathlib import Path


def write_overrides(project_root: Path, model_id: str) -> Path: ...
